<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Link cable na LAN: abre sockets TCP (host/convidado). Sem isto o
         Android bloqueia bind/connect e a conexão não rola. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Mantém o Wi-Fi em baixa latência durante o link (WifiLock). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Suporta gamepad, mas não exige (touch é o padrão). -->
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <application
        android:label="AuroraGBA"
        android:icon="@android:drawable/sym_def_app_icon"
        android:hardwareAccelerated="true"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|navigation"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
